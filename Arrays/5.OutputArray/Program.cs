using System;

class OutputArray
{
    static void Main()
    {
        int[] numbers = { 51, 54, 54, 87, 4, 45, 54, 4 };

        for (int i = 0; i < numbers.Length; i++)
        {
            Console.WriteLine(numbers[i]);
        }
    }
}

